export class MyAnimation {
    constructor(id) {
        this.id = id;
    }

    // Apply here in a way that's the same for other kinds of animations?
    // Not sure why the requirements ask for this
}